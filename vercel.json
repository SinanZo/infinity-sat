{
  "version": 2,
  "buildCommand": "pnpm install --no-frozen-lockfile && pnpm run build && pnpm exec esbuild server/_core/vercel.ts --platform=node --packages=external --bundle --format=cjs --outdir=api",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "outputDirectory": "dist",
  "builds": [
    {
      "src": "api/vercel.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["dist/public/**"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/vercel.js"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
